projection;
strict ( 2 );
use draft;

define behavior for ZMKK_SALESHDR_C //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action setDeliveryStatus;

  use action Edit;
  use action Resume;
  use action Prepare;
  use action Activate;
  use action Discard;

  use association _salesItem { create; with draft; }
}

define behavior for ZMKK_SALESITM_C //alias <alias_name>
{
  use update;
  use delete;

  use association _salesHeader { with draft; }
}